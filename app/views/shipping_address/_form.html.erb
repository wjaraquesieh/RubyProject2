<%= form_with(model: @shipping_address, local: true) do |f| %>
  <div>
    <%= f.label :first_name, 'Nombre' %>
    <%= f.text_field :first_name, class: 'form-control' %>
  </div>
  <!-- Repite lo mismo para todos los campos -->
  <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>
